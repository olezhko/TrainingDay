<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:TrainingDay.Model;assembly=TrainingDay"
             xmlns:viewModel="clr-namespace:TrainingDay.ViewModel;assembly=TrainingDay"
             x:Class="TrainingDay.View.HistoryTrainingPage" Title="{model:Translate LastTrainingsString}">
    <ContentPage.BindingContext>
        <viewModel:TrainingsHistoryViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ListView ItemsSource="{Binding LastTrainings}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell >
                        <StackLayout Orientation="Vertical">
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding ImplementeDateTime}" FontAttributes="Bold"/>
                                <Label Text="{Binding Title}" FontAttributes="Bold"/>
                            </StackLayout>
                            <Label Text="{Binding Description}"/>
                            <Label Text="{Binding ElapsedTime,StringFormat={model:Translate ElapsedTimeString}}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>
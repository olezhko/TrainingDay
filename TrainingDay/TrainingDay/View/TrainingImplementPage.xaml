<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
             xmlns:model="clr-namespace:TrainingDay.Model;assembly=TrainingDay"
             x:Name="Page"
             x:Class="TrainingDay.View.TrainingImplementPage" BindingContext="{x:Reference Name=Page}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{Binding CurrentTime,StringFormat='Время: {0}'}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Items}" SeparatorColor="Black" SeparatorVisibility="Default" HasUnevenRows="True" SelectedItem="{Binding SelectedItem}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Vertical">
                                <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" >
                                    <Label Text="{Binding ExerciseItemName,StringFormat={model:Translate NameString}}" FontSize="Medium" FontAttributes="Bold"/>
                                    <Label Text="{Binding ExerciseCountOfApproches,StringFormat={model:Translate CountOfApprochesString}}" FontSize="Small"/>
                                    <Label Text="{Binding ExerciseCountOfTimes,StringFormat={model:Translate CountOfTimesString}}" FontSize="Small"/>
                                    <Label Text="{Binding ExerciseShortDescription,StringFormat={model:Translate ShortDescriptionString}}" FontSize="Small"/>
                                </StackLayout>
                                <controls:CheckBox Checked="{Binding IsImplemented}" CheckedText="Выполнено"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Завершить" Clicked="FinishButtonClicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
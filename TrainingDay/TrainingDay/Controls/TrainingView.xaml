<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrainingDay.Code.TrainingView">
    <ContentView.Content>
        <Frame OutlineColor="Black">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Text="{Binding Title,StringFormat='Название: {0}'}" Grid.Row="0"/>
                <Label Text="{Binding Description,StringFormat='Описание: {0}'}" Grid.Row="1"/>
                <Frame OutlineColor="DimGray" Grid.Row="2" IsVisible="False" x:Name="FrameListView">
                    <StackLayout x:Name="ExercisesStackLayout" Orientation="Vertical"/> 
                </Frame>
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>
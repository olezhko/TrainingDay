<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="TrainingDay.Views.Controls.AccordionView">
    <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid x:Name="HeaderView">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="Handle_Tapped"/>
                </Grid.GestureRecognizers>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label x:Name="IndicatorLabel" Style="{DynamicResource BodySecondaryBold}" FontSize="Large" VerticalOptions="Center" HorizontalOptions="Center" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Grid.Column="0" Margin="10,0"/>
                <Label x:Name="HeaderLabel" Grid.Column="1" VerticalOptions="Center" LineBreakMode="WordWrap" FontSize="Large" />
            </Grid>
            <Editor x:Name="BodyLabel" Style="{DynamicResource Body}" FontSize="Medium" AutoSize="TextChanges" Grid.Row="1" Margin="0"/>
        </Grid>
    </ContentView.Content>
</ContentView>